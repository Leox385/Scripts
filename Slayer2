local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local VirtualUser = game:GetService("VirtualUser")

local lp = Players.LocalPlayer

if _G.Slayers2Hub then
    _G.Slayers2Hub.Running = false
    task.wait(0.2)
end

local State = {
    Running = true,
    AntiAFK = false,
    AutoFarmMobs = false,
    SelectedMob = "Bandit",
    MobHeightOffset = 11.0,
    MobAttackDelay = 0.1,
    AutoFarmBoss = false,
    SelectedBoss = "Akazo",
    Godmode = false,
    DefenseMode = "Sky God-Hover (Directly Above)",
    BossHeightOffset = 11.0,
    BossBehindDistance = 6.0,
    AutoParry = false,
    HealthSafeguard = false,
    BossAttackDelay = 0.1,
    FastAttack = false,
    AutoQuestTasks = false,
    SelectedQuest = "[Windy Peak] Krue: 3 Bandits",
    QuestReturnAfter = false,
    AutoCompleteTraining = false,
    AutoSpamSkills = false,
    SkillSpamInterval = 2.0,
    NoClip = false,
    WalkSpeed = 16
}
_G.Slayers2Hub = State

local remSuccess, _ = pcall(function()
    local remCode = game:HttpGet("https://raw.githubusercontent.com/9mfg/rem-ui/main/rem.lua")
    
    local imgBytes = nil
    pcall(function()
        imgBytes = game:HttpGet("https://files.catbox.moe/hiuvyz.png")
    end)
    
    if imgBytes and #imgBytes > 0 then
        _G.CustomRemImageData = imgBytes
        remCode = remCode:gsub("image%.Data=base64decode%(REM_IMAGE%)", "image.Data=_G.CustomRemImageData or base64decode(REM_IMAGE)")
    end
    
    remCode = remCode:gsub(
        "image%.Position=V%(x,y%);image%.Size=V%(W%*S,H%*S%);image%.Rounding=17%*S;image%.Transparency=0%.48%*a;image%.ZIndex=11;image%.Visible=true",
        "local cs=165*S;image.Position=V(x+(W-175)*S,y+(H-175)*S);image.Size=V(cs,cs);image.Rounding=14*S;image.Transparency=0.88*a;image.ZIndex=11;image.Visible=true"
    )
    
    loadstring(remCode)()
end)

local UI = Rem or getfenv().Rem or _G.Rem
if not UI or not UI.Alive then
    return
end

local SignalEvent = ReplicatedStorage:FindFirstChild("Communication")
    and ReplicatedStorage.Communication:FindFirstChild("ServerAndClient")
    and ReplicatedStorage.Communication.ServerAndClient:FindFirstChild("Signals")
    and ReplicatedStorage.Communication.ServerAndClient.Signals:FindFirstChild("SignalEvent")
local RemoteEvent = SignalEvent and SignalEvent:FindFirstChild("Event")

local RegionSpawns = {
    ["Bandit"] = Vector3.new(-287, 1224, -1008),
    ["Zuko"] = Vector3.new(-450, 1245, -950),
    ["Civilian"] = Vector3.new(-650, 1245, -1100),
    ["*Civilian*"] = Vector3.new(-650, 1245, -1100),
    ["Mizunoto"] = Vector3.new(-888, 965, -26),
    ["Beast Born Demon"] = Vector3.new(-850, 970, -120),
    ["Blood Hounded Demon"] = Vector3.new(-870, 970, -100),
    ["Bear Cub"] = Vector3.new(540, 1122, -1023),
    ["Mother Bear"] = Vector3.new(550, 1122, -1035),
    ["Hoyuzo"] = Vector3.new(480, 1122, -1010),
    ["Hoyuzo Subordinate"] = Vector3.new(470, 1122, -1010),
    ["Kaiden"] = Vector3.new(510, 1122, -1000),
    ["Kaiden Subordinate"] = Vector3.new(500, 1122, -1000),
    ["Lesser Demon"] = Vector3.new(-715, 220, 456),
    ["Greater Demon"] = Vector3.new(-650, 235, 380),
    ["High Demon"] = Vector3.new(518, 1223, -1785),
    ["Ice Profound Demon"] = Vector3.new(312, 1223, -1778),
    ["Fire Profound Demon"] = Vector3.new(230, 1254, -1983),
    ["Kanoe Demon Slayer"] = Vector3.new(373, 1286, -2031),
    ["Mizunoe Demon Slayer"] = Vector3.new(-1898, 30, 487),
    ["Fujiko"] = Vector3.new(-1800, 30, 500),
    ["Akazo"] = Vector3.new(-1132.6, 1381.1, -1742.5),
    ["Gyutai"] = Vector3.new(-263.1, 1043.5, -1150.5),
    ["Datai"] = Vector3.new(-163.2, 1043.5, -1136.9),
    ["Saneri"] = Vector3.new(-375.2, 1093.6, -438.8),
    ["Serpent Trainee"] = Vector3.new(-171.3, 1279.0, -1475.1)
}

local RegularMobsList = {
    "Bandit",
    "Civilian",
    "*Civilian*",
    "Mizunoto",
    "Beast Born Demon",
    "Blood Hounded Demon",
    "Bear Cub",
    "Mother Bear",
    "Hoyuzo Subordinate",
    "Kaiden Subordinate",
    "Lesser Demon",
    "Greater Demon",
    "High Demon",
    "Ice Profound Demon",
    "Fire Profound Demon",
    "Kanoe Demon Slayer",
    "Mizunoe Demon Slayer",
    "Serpent Trainee",
    "Cache Lancer",
    "Cache Prowler",
    "All Regular Mobs"
}

local BossMobsList = {
    "Akazo",
    "Gyutai",
    "Datai",
    "Saneri",
    "Zuko",
    "Kaiden",
    "Hoyuzo",
    "Mother Bear",
    "Fujiko",
    "All Bosses"
}

local QuestRegistry = {
    ["[Windy Peak] Krue: 3 Bandits"] = {
        NpcName = "Krue",
        NpcPos = Vector3.new(-425.9, 1240.7, -952.5),
        Region = "Windy Peak",
        TargetEnemy = "Bandit",
        Description = "Defeat 3 Bandits at the mountain camp."
    },
    ["[Windy Peak] Krue: Bandit Leader Zuko"] = {
        NpcName = "Krue",
        NpcPos = Vector3.new(-425.9, 1240.7, -952.5),
        Region = "Windy Peak",
        TargetEnemy = "Zuko",
        Description = "Defeat the Bandit Leader Zuko."
    },
    ["[Windy Peak] Lucy: Bear Meat"] = {
        NpcName = "Lucy",
        NpcPos = Vector3.new(-615.5, 1260.2, -1177.5),
        Region = "Bamboo Grove",
        TargetEnemy = "Mother Bear",
        AltTarget = "Bear Cub",
        Description = "Hunt bears to obtain meat for Lucy."
    },
    ["[Mistfall Harbor] Niko: Eliminate Mizunoto"] = {
        NpcName = "Estate Worker Niko",
        NpcPos = Vector3.new(341.1, 938.0, 581.9),
        Region = "Mistfall Harbor",
        TargetEnemy = "Mizunoto",
        Description = "Eliminate Mizunoto at the harbor docks."
    },
    ["[Mistfall Harbor] Niko: 5 Broken Blades"] = {
        NpcName = "Estate Worker Niko",
        NpcPos = Vector3.new(341.1, 938.0, 581.9),
        Region = "Mistfall Harbor",
        TargetEnemy = "Mizunoto",
        Description = "Collect 5 Broken Blades from defeated Mizunoto."
    },
    ["[Mistfall Harbor] Ginzo: Lost Jewelry Box"] = {
        NpcName = "Ginzo",
        NpcPos = Vector3.new(273.8, 944.0, 528.2),
        Region = "Mistfall Harbor",
        TargetEnemy = "Mizunoto",
        Description = "Recover Ginzo's stolen jewelry box."
    },
    ["[Bamboo Grove] Betty: Betty's Gemstone"] = {
        NpcName = "Betty",
        NpcPos = Vector3.new(714.0, 1125.1, -808.0),
        Region = "Bamboo Grove",
        TargetEnemy = "Hoyuzo Subordinate",
        AltTarget = "Kaiden Subordinate",
        Description = "Find Betty's lost gemstone in the bamboo forest."
    },
    ["[Bamboo Grove] Tom: Wood & Bamboo"] = {
        NpcName = "Tom",
        NpcPos = Vector3.new(508.5, 1124.3, -970.5),
        Region = "Bamboo Grove",
        TargetEnemy = "Hoyuzo",
        Description = "Assist Tom in the bamboo grove."
    },
    ["[Butterfly Estate] Shiori: Restock Infirmary"] = {
        NpcName = "Shiori",
        NpcPos = Vector3.new(-873.2, 236.4, 318.7),
        Region = "Butterfly Estate",
        TargetEnemy = "Lesser Demon",
        AltTarget = "Greater Demon",
        Description = "Defeat demons to secure medical herbs for the infirmary."
    },
    ["[Butterfly Estate] Ren: Lost Nichirin"] = {
        NpcName = "Ren",
        NpcPos = Vector3.new(-873.2, 236.4, 318.7),
        Region = "Butterfly Estate",
        TargetEnemy = "Greater Demon",
        AltTarget = "Lesser Demon",
        Description = "Recover Ren's lost Nichirin sword from demons."
    },
    ["[Iceveil Valley] Tomoi: Ice Demons"] = {
        NpcName = "Wounded Slayer Tomoi",
        NpcPos = Vector3.new(485.3, 1222.9, -1813.4),
        Region = "Iceveil Valley",
        TargetEnemy = "Ice Profound Demon",
        AltTarget = "High Demon",
        Description = "Eliminate high-ranking ice demons in Iceveil."
    },
    ["[Iceveil Valley] Mitsu: Fire Demons"] = {
        NpcName = "Wounded Slayer Tomoi",
        NpcPos = Vector3.new(485.3, 1222.9, -1813.4),
        Region = "Iceveil Valley",
        TargetEnemy = "Fire Profound Demon",
        AltTarget = "High Demon",
        Description = "Eliminate fire demons lurking in the frozen valley."
    },
    ["[Final Selection] Exam: Mizunoe"] = {
        NpcName = "Demon Mokuro",
        NpcPos = Vector3.new(-1800, 30, 500),
        Region = "Final Selection Plains",
        TargetEnemy = "Mizunoe Demon Slayer",
        AltTarget = "Fujiko",
        Description = "Defeat final selection targets."
    },
    ["[Misc] Muzan: Demon Quest"] = {
        NpcName = "Muzan",
        NpcPos = Vector3.new(182.1, 875.0, 697.0),
        Region = "Misc",
        TargetEnemy = "Kanoe Demon Slayer",
        AltTarget = "Mizunoto",
        Description = "Fulfill the bidding of the Demon King Muzan."
    },
    ["[Auto-Detect Any Active Quest]"] = {
        AutoDetect = true,
        Description = "Automatically detects whatever quest is active on your character."
    }
}

local QuestOptions = {
    "[Windy Peak] Krue: 3 Bandits",
    "[Windy Peak] Krue: Bandit Leader Zuko",
    "[Windy Peak] Lucy: Bear Meat",
    "[Mistfall Harbor] Niko: Eliminate Mizunoto",
    "[Mistfall Harbor] Niko: 5 Broken Blades",
    "[Mistfall Harbor] Ginzo: Lost Jewelry Box",
    "[Bamboo Grove] Betty: Betty's Gemstone",
    "[Bamboo Grove] Tom: Wood & Bamboo",
    "[Butterfly Estate] Shiori: Restock Infirmary",
    "[Butterfly Estate] Ren: Lost Nichirin",
    "[Iceveil Valley] Tomoi: Ice Demons",
    "[Iceveil Valley] Mitsu: Fire Demons",
    "[Final Selection] Exam: Mizunoe",
    "[Misc] Muzan: Demon Quest",
    "[Auto-Detect Any Active Quest]"
}

local TeleportZones = {
    ["Windy Peak (Main Village)"] = Vector3.new(-650, 1245, -1100),
    ["Bandit Camp (Windy Peak)"] = Vector3.new(-287, 1224, -1008),
    ["Mistfall Harbor (Docks)"] = Vector3.new(-888, 965, -26),
    ["Bamboo Grove (Bear Woods)"] = Vector3.new(540, 1122, -1023),
    ["Butterfly Estate (Lesser Demons)"] = Vector3.new(-715, 220, 456),
    ["Iceveil Valley (Frozen Valley)"] = Vector3.new(518, 1223, -1785),
    ["Final Selection Plains (Exam)"] = Vector3.new(-1898, 30, 487),
    ["Bosses & Trainees Zone (Misc)"] = Vector3.new(-263, 1044, -1150),
    ["Workout Grounds (Boulder Push)"] = Vector3.new(-306.3, 1071.0, -593.9)
}

local BossTeleports = {
    ["Akazo (Upper Rank)"] = Vector3.new(-1132.6, 1381.1, -1742.5),
    ["Gyutai"] = Vector3.new(-263.1, 1043.5, -1150.5),
    ["Datai"] = Vector3.new(-163.2, 1043.5, -1136.9),
    ["Saneri"] = Vector3.new(-375.2, 1093.6, -438.8),
    ["Zuko"] = Vector3.new(-450, 1245, -950),
    ["Kaiden"] = Vector3.new(510, 1122, -1000),
    ["Hoyuzo"] = Vector3.new(480, 1122, -1010),
    ["Mother Bear"] = Vector3.new(550, 1122, -1035),
    ["Fujiko"] = Vector3.new(-1800, 30, 500)
}

local TrainerTeleports = {
    ["Serpent Trainer Obari"] = Vector3.new(37.0, 1307.5, -1179.5),
    ["Serpent Trainee"] = Vector3.new(-171.3, 1279.0, -1475.1),
    ["Water Trainee Sabito"] = Vector3.new(-888, 965, -26),
    ["Thunder Trainee"] = Vector3.new(-300, 1224, -900),
    ["Flame Trainee"] = Vector3.new(-250, 1224, -850),
    ["Wind Trainee"] = Vector3.new(-350, 1224, -950),
    ["Stone Trainee"] = Vector3.new(-1041.4, 1131.0, -589.3),
    ["Insect Trainee"] = Vector3.new(-715, 220, 456),
    ["Sound Trainee"] = Vector3.new(-400, 1200, -800),
    ["Soryu Trainee Goki"] = Vector3.new(540, 1122, -1023),
    ["Tai Chi Trainee Suzume"] = Vector3.new(500, 1122, -1000),
    ["Reaper Trainee Kuzan"] = Vector3.new(300, 1223, -1700)
}

local WorkoutTeleports = {
    ["Boulder Push (Strength)"] = Vector3.new(-306.3, 1071.0, -593.9),
    ["Boulder Split (Attack Power)"] = Vector3.new(-1041.4, 1131.0, -589.3),
    ["Pushups (Endurance)"] = Vector3.new(-306.3, 1071.0, -620.0),
    ["Meditation (Breathing)"] = Vector3.new(-350.0, 1071.0, -594.0),
    ["Squat Rack (Legs / Agility)"] = Vector3.new(-320.0, 1071.0, -594.0),
    ["Cup Game (Reflexes)"] = Vector3.new(-280.0, 1071.0, -594.0),
    ["Aim Training (Aim Accuracy)"] = Vector3.new(-260.0, 1071.0, -594.0),
    ["Parkour Dungeon"] = Vector3.new(-400.0, 1071.0, -594.0)
}

local function getTargets(targetName)
    local results = {}
    local reg = Workspace.Humanoids:FindFirstChild("Regions")
    if not reg then return results end
    for _, r in ipairs(reg:GetChildren()) do
        local active = r:FindFirstChild("ActiveNpcs")
        if active then
            for _, item in ipairs(active:GetChildren()) do
                local match = false
                if targetName == "All Regular Mobs" or targetName == "All Bosses" or targetName == "All Enemies" then
                    match = true
                elseif item.Name == targetName then
                    match = true
                end
                
                if match then
                    local m = item:FindFirstChildOfClass("Model") or (item.ClassName == "Model" and item)
                    if not m then
                        for _, sub in ipairs(item:GetChildren()) do
                            if sub.ClassName == "Model" and sub:FindFirstChildOfClass("Humanoid") then
                                m = sub
                                break
                            end
                        end
                    end
                    if m then
                        local hum = m:FindFirstChildOfClass("Humanoid")
                        local root = m:FindFirstChild("HumanoidRootPart") or m:FindFirstChildWhichIsA("BasePart")
                        if hum and root and hum.Health > 0 then
                            table.insert(results, {
                                Model = m,
                                Humanoid = hum,
                                Root = root,
                                Name = item.Name
                            })
                        end
                    end
                end
            end
        end
    end
    return results
end

local function getEquippedStyle()
    local char = lp.Character
    if not char then return "Combat" end
    for _, c in ipairs(char:GetChildren()) do
        if c.ClassName == "Tool" then
            return c.Name
        end
    end
    return "Combat"
end

local function safeTeleport(pos)
    local char = lp.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    if hrp and pos then
        hrp.CFrame = CFrame.new(pos.X, pos.Y + 2, pos.Z)
        hrp.AssemblyLinearVelocity = Vector3.zero
    end
end

local function getActiveQuest()
    local pData = ReplicatedStorage:FindFirstChild("Player_Service")
        and ReplicatedStorage.Player_Service:FindFirstChild("Data")
        and ReplicatedStorage.Player_Service.Data:FindFirstChild(lp.Name)
    local holder = pData and pData:FindFirstChild("slots")
        and pData.slots:FindFirstChild("Slot1")
        and pData.slots.Slot1:FindFirstChild("Quests")
        and pData.slots.Slot1.Quests:FindFirstChild("Holder")
    if not holder then return nil end
    for _, q in ipairs(holder:GetChildren()) do
        local tasksFolder = q:FindFirstChild("Tasks")
        if tasksFolder then
            local tasks = {}
            local isAllComplete = true
            for _, t in ipairs(tasksFolder:GetChildren()) do
                local val = t:FindFirstChild("Value") and t.Value.Value or 0
                local max = t:FindFirstChild("Max") and t.Max.Value or 1
                table.insert(tasks, {Name = t.Name, Value = val, Max = max})
                if val < max then
                    isAllComplete = false
                end
            end
            local qStr = q:FindFirstChild("QuestString") and q.QuestString.Value or q.Name
            return {
                Folder = q,
                Name = qStr,
                Id = q.Name,
                Tasks = tasks,
                Complete = isAllComplete
            }
        end
    end
    return nil
end

local FarmMobsTab = UI:AddTab({ Title = "Auto-Farm (Mobs)", Icon = "script" })
local FarmBossTab = UI:AddTab({ Title = "Boss Farm & Defense", Icon = "script" })
local QuestTab = UI:AddTab({ Title = "Quests", Icon = "script" })
local TeleportTab = UI:AddTab({ Title = "Teleports", Icon = "gear" })
local TrainTab = UI:AddTab({ Title = "Training", Icon = "script" })
local MiscTab = UI:AddTab({ Title = "Settings", Icon = "gear" })

FarmMobsTab:AddDropdown({
    Title = "Select Mob",
    Options = RegularMobsList,
    Default = "Bandit",
    Callback = function(val)
        State.SelectedMob = val
        UI:Notify({
            Title = "Mob Selected",
            Content = tostring(val),
            Type = "info",
            Duration = 2
        })
    end
})

FarmMobsTab:AddToggle({
    Title = "Auto-Farm Selected Mob",
    Default = false,
    Callback = function(val)
        State.AutoFarmMobs = val
        if val then
            State.AutoFarmBoss = false
            UI:Notify({ Title = "Mob Farm", Content = "Active", Type = "success", Duration = 3 })
        else
            UI:Notify({ Title = "Mob Farm", Content = "Paused", Type = "warn", Duration = 2 })
        end
    end
})

FarmMobsTab:AddSlider({
    Title = "Mob Hover Height",
    Min = 3,
    Max = 15,
    Default = 5,
    Step = 0.5,
    Callback = function(val)
        State.MobHeightOffset = val
    end
})

FarmMobsTab:AddSlider({
    Title = "Attack Delay (ms)",
    Min = 50,
    Max = 250,
    Default = 100,
    Step = 10,
    Callback = function(val)
        State.MobAttackDelay = val / 1000
    end
})

FarmMobsTab:AddButton({
    Title = "Scan Mobs on Server",
    ButtonText = "Scan",
    Callback = function()
        local targets = getTargets(State.SelectedMob)
        UI:Notify({
            Title = "Mob Scan",
            Content = string.format("Found %d alive", #targets),
            Type = "info",
            Duration = 4
        })
    end
})

FarmBossTab:AddDropdown({
    Title = "Select Boss",
    Options = BossMobsList,
    Default = "Akazo",
    Callback = function(val)
        State.SelectedBoss = val
        UI:Notify({
            Title = "Boss Selected",
            Content = tostring(val),
            Type = "info",
            Duration = 2
        })
    end
})

FarmBossTab:AddToggle({
    Title = "Auto-Farm Selected Boss",
    Default = false,
    Callback = function(val)
        State.AutoFarmBoss = val
        if val then
            State.AutoFarmMobs = false
            UI:Notify({ Title = "Boss Farm", Content = "Active", Type = "success", Duration = 3 })
        else
            if keyrelease then pcall(function() keyrelease(Enum.KeyCode.F.Value) end) end
            UI:Notify({ Title = "Boss Farm", Content = "Paused", Type = "warn", Duration = 2 })
        end
    end
})

FarmBossTab:AddDropdown({
    Title = "Defense Mode",
    Options = { "Sky God-Hover (Directly Above)", "Behind Boss (Orbit)" },
    Default = "Sky God-Hover (Directly Above)",
    Callback = function(val)
        State.DefenseMode = val
    end
})

FarmBossTab:AddToggle({
    Title = "Godmode (Hitbox & Touch Invisibility)",
    Default = false,
    Callback = function(val)
        State.Godmode = val
    end
})

FarmBossTab:AddToggle({
    Title = "Hold Auto-Parry / Guard (Key F)",
    Default = false,
    Callback = function(val)
        State.AutoParry = val
        if not val and keyrelease then
            pcall(function() keyrelease(Enum.KeyCode.F.Value) end)
        end
    end
})

FarmBossTab:AddToggle({
    Title = "Emergency Health Safeguard",
    Default = false,
    Callback = function(val)
        State.HealthSafeguard = val
    end
})

FarmBossTab:AddSlider({
    Title = "Boss Hover Height",
    Min = 4,
    Max = 20,
    Default = 5,
    Step = 0.5,
    Callback = function(val)
        State.BossHeightOffset = val
    end
})

FarmBossTab:AddSlider({
    Title = "Boss Distance Offset",
    Min = 2,
    Max = 12,
    Default = 5,
    Step = 0.5,
    Callback = function(val)
        State.BossBehindDistance = val
    end
})

FarmBossTab:AddButton({
    Title = "Scan Bosses on Server",
    ButtonText = "Scan",
    Callback = function()
        local targets = getTargets(State.SelectedBoss)
        UI:Notify({
            Title = "Boss Scan",
            Content = string.format("Found %d alive", #targets),
            Type = "info",
            Duration = 4
        })
    end
})

QuestTab:AddDropdown({
    Title = "Select Quest",
    Options = QuestOptions,
    Default = State.SelectedQuest,
    Callback = function(val)
        State.SelectedQuest = val
    end
})

QuestTab:AddButton({
    Title = "Teleport to Quest NPC (Accept)",
    ButtonText = "Go to NPC",
    Callback = function()
        local data = QuestRegistry[State.SelectedQuest]
        if data and data.NpcPos then
            safeTeleport(data.NpcPos)
            UI:Notify({
                Title = "Arrived at NPC",
                Content = tostring(data.NpcName),
                Type = "success",
                Duration = 3
            })
        else
            UI:Notify({
                Title = "NPC Undefined",
                Content = "Select a quest first",
                Type = "warn",
                Duration = 3
            })
        end
    end
})

QuestTab:AddToggle({
    Title = "Auto-Complete Quest Tasks",
    Default = false,
    Callback = function(val)
        State.AutoQuestTasks = val
        if val then
            UI:Notify({
                Title = "Task Farming",
                Content = "Active",
                Type = "success",
                Duration = 3
            })
        else
            UI:Notify({
                Title = "Task Farming",
                Content = "Paused",
                Type = "warn",
                Duration = 2
            })
        end
    end
})

QuestTab:AddToggle({
    Title = "Auto-Return to NPC on Finish",
    Default = false,
    Callback = function(val)
        State.QuestReturnAfter = val
    end
})

QuestTab:AddButton({
    Title = "Teleport to Quest NPC (Claim Reward)",
    ButtonText = "Go to Claim",
    Callback = function()
        local data = QuestRegistry[State.SelectedQuest]
        if data and data.NpcPos then
            safeTeleport(data.NpcPos)
            UI:Notify({
                Title = "Arrived at NPC",
                Content = tostring(data.NpcName),
                Type = "success",
                Duration = 3
            })
        end
    end
})

QuestTab:AddButton({
    Title = "Check Active Quest Progress",
    ButtonText = "Check Progress",
    Callback = function()
        local activeQ = getActiveQuest()
        if activeQ then
            local taskList = {}
            for _, t in ipairs(activeQ.Tasks) do
                table.insert(taskList, string.format("%s: %d/%d", t.Name, t.Value, t.Max))
            end
            local statusStr = table.concat(taskList, " | ")
            local isDone = activeQ.Complete and " (READY TO CLAIM!)" or " (In Progress)"
            UI:Notify({
                Title = activeQ.Name .. isDone,
                Content = statusStr,
                Type = activeQ.Complete and "success" or "info",
                Duration = 5
            })
        else
            UI:Notify({
                Title = "No Active Quest",
                Content = "No quest found",
                Type = "warn",
                Duration = 3
            })
        end
    end
})

local ZoneNames = {}
for k, _ in pairs(TeleportZones) do table.insert(ZoneNames, k) end
table.sort(ZoneNames)

local SelectedZone = ZoneNames[1]
TeleportTab:AddDropdown({
    Title = "Region",
    Options = ZoneNames,
    Default = SelectedZone,
    Callback = function(val) SelectedZone = val end
})

TeleportTab:AddButton({
    Title = "Travel to Region",
    ButtonText = "Teleport",
    Callback = function()
        local pos = TeleportZones[SelectedZone]
        if pos then
            safeTeleport(pos)
        end
    end
})

local BossNames = {}
for k, _ in pairs(BossTeleports) do table.insert(BossNames, k) end
table.sort(BossNames)

local SelectedBossTeleport = BossNames[1]
TeleportTab:AddDropdown({
    Title = "Boss Arena",
    Options = BossNames,
    Default = SelectedBossTeleport,
    Callback = function(val) SelectedBossTeleport = val end
})

TeleportTab:AddButton({
    Title = "Travel to Boss Arena",
    ButtonText = "Teleport",
    Callback = function()
        local pos = BossTeleports[SelectedBossTeleport]
        if pos then
            safeTeleport(pos)
        end
    end
})

local TrainerNames = {}
for k, _ in pairs(TrainerTeleports) do table.insert(TrainerNames, k) end
table.sort(TrainerNames)

local SelectedTrainer = TrainerNames[1]
TeleportTab:AddDropdown({
    Title = "Trainer",
    Options = TrainerNames,
    Default = SelectedTrainer,
    Callback = function(val) SelectedTrainer = val end
})

TeleportTab:AddButton({
    Title = "Travel to Trainer",
    ButtonText = "Teleport",
    Callback = function()
        local pos = TrainerTeleports[SelectedTrainer]
        if pos then
            safeTeleport(pos)
        end
    end
})

local WorkoutNames = {}
for k, _ in pairs(WorkoutTeleports) do table.insert(WorkoutNames, k) end
table.sort(WorkoutNames)

local SelectedWorkout = WorkoutNames[1]
TrainTab:AddDropdown({
    Title = "Workout Station",
    Options = WorkoutNames,
    Default = SelectedWorkout,
    Callback = function(val) SelectedWorkout = val end
})

TrainTab:AddButton({
    Title = "Go to Workout",
    ButtonText = "Teleport",
    Callback = function()
        local pos = WorkoutTeleports[SelectedWorkout]
        if pos then
            safeTeleport(pos)
        end
    end
})

TrainTab:AddToggle({
    Title = "Auto-Train (Auto-Press T & Complete)",
    Default = false,
    Callback = function(val)
        State.AutoCompleteTraining = val
        if val then
            UI:Notify({ Title = "Auto-Training", Content = "Active (Pressing T & Completing)", Type = "success", Duration = 3 })
        else
            if keyrelease then
                pcall(function()
                    keyrelease(116)
                    keyrelease(84)
                end)
            end
            UI:Notify({ Title = "Auto-Training", Content = "Paused", Type = "warn", Duration = 2 })
        end
    end
})

MiscTab:AddToggle({
    Title = "Anti-AFK",
    Default = false,
    Callback = function(val)
        State.AntiAFK = val
    end
})

MiscTab:AddToggle({
    Title = "Global Godmode",
    Default = false,
    Callback = function(val)
        State.Godmode = val
    end
})

MiscTab:AddToggle({
    Title = "Fast Attack (Global)",
    Default = false,
    Callback = function(val)
        State.FastAttack = val
    end
})

MiscTab:AddToggle({
    Title = "NoClip / Float",
    Default = false,
    Callback = function(val)
        State.NoClip = val
    end
})

MiscTab:AddSlider({
    Title = "WalkSpeed Modifier",
    Min = 16,
    Max = 120,
    Default = 16,
    Step = 2,
    Callback = function(val)
        State.WalkSpeed = val
        local char = lp.Character
        local hum = char and char:FindFirstChildOfClass("Humanoid")
        if hum then hum.WalkSpeed = val end
    end
})

MiscTab:AddButton({
    Title = "Emergency Stop All",
    ButtonText = "Stop All",
    Callback = function()
        State.Running = false
        State.AutoFarmMobs = false
        State.AutoFarmBoss = false
        State.AutoQuestTasks = false
        State.FastAttack = false
        State.AutoSpamSkills = false
        if keyrelease then pcall(function() keyrelease(Enum.KeyCode.F.Value) end) end
        UI:Notify({ Title = "Hub", Content = "Stopped", Type = "warn", Duration = 3 })
    end
})

FarmMobsTab:Select()

pcall(function()
    local player = lp or Players.LocalPlayer
    if player then
        pcall(function()
            if getconnections and player.Idled then
                for _, conn in pairs(getconnections(player.Idled)) do
                    if conn.Disable then
                        conn:Disable()
                    elseif conn.Disconnect then
                        conn:Disconnect()
                    end
                end
            end
        end)
        
        pcall(function()
            if player.Idled then
                player.Idled:Connect(function()
                    if State.AntiAFK then
                        pcall(function()
                            VirtualUser:CaptureController()
                            VirtualUser:ClickButton2(Vector2.new())
                        end)
                    end
                end)
            end
        end)
    end
end)

task.spawn(function()
    while State.Running do
        if State.AntiAFK then
            pcall(function()
                VirtualUser:CaptureController()
                VirtualUser:ClickButton2(Vector2.new(100, 100))
            end)
            pcall(function()
                local cam = workspace.CurrentCamera
                VirtualUser:Button2Down(Vector2.new(0, 0), cam and cam.CFrame or CFrame.new())
                task.wait(0.05)
                VirtualUser:Button2Up(Vector2.new(0, 0), cam and cam.CFrame or CFrame.new())
            end)
        end
        task.wait(90)
    end
end)

task.spawn(function()
    while State.Running do
        if State.Godmode then
            local char = lp.Character
            if char then
                for _, part in ipairs(char:GetChildren()) do
                    if part:IsA("BasePart") then
                        pcall(function()
                            part.CanTouch = false
                            part.CanQuery = false
                        end)
                        if State.NoClip and part.Name ~= "HumanoidRootPart" then
                            part.CanCollide = false
                        end
                    end
                end
            end
        end
        task.wait(0.15)
    end
end)

task.spawn(function()
    local combo = 1
    while State.Running do
        if State.AutoFarmMobs then
            local char = lp.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            local hrp = char and char:FindFirstChild("HumanoidRootPart")
            
            if hum and hrp and hum.Health > 0 then
                local targets = getTargets(State.SelectedMob)
                local target = nil
                
                local bestDist = math.huge
                for _, t in ipairs(targets) do
                    local d = (t.Root.Position - hrp.Position).Magnitude
                    if d < bestDist then
                        bestDist = d
                        target = t
                    end
                end
                
                if target and target.Humanoid.Health > 0 then
                    local style = getEquippedStyle()
                    while State.Running and State.AutoFarmMobs and target.Humanoid and target.Humanoid.Health > 0 do
                        local tPos = target.Root.Position
                        hrp.CFrame = CFrame.lookAt(tPos + Vector3.new(0, State.MobHeightOffset, 0), tPos)
                        hrp.AssemblyLinearVelocity = Vector3.zero
                        
                        if RemoteEvent then
                            RemoteEvent:FireServer("Combat_Service", style, combo, false, 0, false, nil)
                        end
                        if mouse1click then mouse1click() end
                        combo = combo % 5 + 1
                        task.wait(State.MobAttackDelay)
                    end
                else
                    local spawnPos = RegionSpawns[State.SelectedMob]
                    if spawnPos then
                        safeTeleport(spawnPos)
                        task.wait(1.2)
                    else
                        task.wait(0.5)
                    end
                end
            end
        end
        task.wait(0.1)
    end
end)

task.spawn(function()
    local combo = 1
    while State.Running do
        if State.AutoFarmBoss then
            local char = lp.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            local hrp = char and char:FindFirstChild("HumanoidRootPart")
            
            if hum and hrp and hum.Health > 0 then
                if State.HealthSafeguard and hum.Health < hum.MaxHealth * 0.5 then
                    hrp.CFrame = hrp.CFrame + Vector3.new(0, 45, 0)
                    hrp.AssemblyLinearVelocity = Vector3.zero
                    while State.Running and State.AutoFarmBoss and hum.Health < hum.MaxHealth * 0.9 do
                        task.wait(1.0)
                    end
                end
                
                local targets = getTargets(State.SelectedBoss)
                local target = nil
                
                local bestDist = math.huge
                for _, t in ipairs(targets) do
                    local d = (t.Root.Position - hrp.Position).Magnitude
                    if d < bestDist then
                        bestDist = d
                        target = t
                    end
                end
                
                if target and target.Humanoid.Health > 0 then
                    local style = getEquippedStyle()
                    
                    if State.AutoParry and keypress then
                        pcall(function() keypress(Enum.KeyCode.F.Value) end)
                    end
                    
                    while State.Running and State.AutoFarmBoss and target.Humanoid and target.Humanoid.Health > 0 do
                        if State.HealthSafeguard and hum.Health < hum.MaxHealth * 0.5 then
                            hrp.CFrame = hrp.CFrame + Vector3.new(0, 45, 0)
                            hrp.AssemblyLinearVelocity = Vector3.zero
                            while State.Running and State.AutoFarmBoss and hum.Health < hum.MaxHealth * 0.9 do
                                task.wait(1.0)
                            end
                            break
                        end
                        
                        local bRoot = target.Root
                        local bPos = bRoot.Position
                        
                        if State.DefenseMode == "Sky God-Hover (Directly Above)" then
                            local skyPos = bPos + Vector3.new(0, State.BossHeightOffset, 0)
                            hrp.CFrame = CFrame.lookAt(skyPos, bPos)
                        else
                            local behindPos = (bRoot.CFrame * CFrame.new(0, State.BossHeightOffset, State.BossBehindDistance)).Position
                            hrp.CFrame = CFrame.lookAt(behindPos, bPos)
                        end
                        hrp.AssemblyLinearVelocity = Vector3.zero
                        
                        if RemoteEvent then
                            RemoteEvent:FireServer("Combat_Service", style, combo, false, 0, false, nil)
                        end
                        if mouse1click then mouse1click() end
                        combo = combo % 5 + 1
                        task.wait(State.BossAttackDelay)
                    end
                    
                    if State.AutoParry and keyrelease then
                        pcall(function() keyrelease(Enum.KeyCode.F.Value) end)
                    end
                else
                    local spawnPos = RegionSpawns[State.SelectedBoss]
                    if spawnPos then
                        safeTeleport(spawnPos)
                        task.wait(1.2)
                    else
                        task.wait(0.5)
                    end
                end
            end
        else
            task.wait(0.2)
        end
    end
end)

task.spawn(function()
    local combo = 1
    local lastNotifiedComplete = nil
    
    while State.Running do
        if State.AutoQuestTasks then
            local activeQ = getActiveQuest()
            local qData = QuestRegistry[State.SelectedQuest]
            
            if activeQ then
                if activeQ.Complete then
                    if lastNotifiedComplete ~= activeQ.Name then
                        lastNotifiedComplete = activeQ.Name
                        UI:Notify({
                            Title = "Quest Objectives Done!",
                            Content = tostring(activeQ.Name),
                            Type = "success",
                            Duration = 4
                        })
                        
                        if State.QuestReturnAfter and qData and qData.NpcPos then
                            safeTeleport(qData.NpcPos)
                            State.AutoQuestTasks = false
                        end
                    end
                    task.wait(1.5)
                else
                    lastNotifiedComplete = nil
                    
                    local targetMob = nil
                    if qData and not qData.AutoDetect and qData.TargetEnemy then
                        targetMob = qData.TargetEnemy
                    end
                    
                    if not targetMob then
                        local qNameLower = activeQ.Name:lower()
                        for _, t in ipairs(activeQ.Tasks) do
                            if t.Value < t.Max then
                                local tLower = t.Name:lower()
                                if tLower:find("bandit") or qNameLower:find("bandit") or qNameLower:find("krue") then
                                    targetMob = "Bandit"
                                elseif tLower:find("zuko") or qNameLower:find("zuko") then
                                    targetMob = "Zuko"
                                elseif tLower:find("mizunoto") or qNameLower:find("niko") or tLower:find("blade") then
                                    targetMob = "Mizunoto"
                                elseif tLower:find("bear") or qNameLower:find("lucy") or tLower:find("meat") then
                                    targetMob = "Mother Bear"
                                elseif tLower:find("ice") or tLower:find("profound") then
                                    targetMob = "Ice Profound Demon"
                                elseif tLower:find("fire") then
                                    targetMob = "Fire Profound Demon"
                                elseif tLower:find("high demon") then
                                    targetMob = "High Demon"
                                elseif tLower:find("demon") or qNameLower:find("shiori") then
                                    targetMob = "Lesser Demon"
                                elseif tLower:find("mizunoe") then
                                    targetMob = "Mizunoe Demon Slayer"
                                elseif tLower:find("kanoe") then
                                    targetMob = "Kanoe Demon Slayer"
                                end
                                if targetMob then break end
                            end
                        end
                    end
                    
                    if not targetMob then targetMob = "Bandit" end
                    
                    local char = lp.Character
                    local hum = char and char:FindFirstChildOfClass("Humanoid")
                    local hrp = char and char:FindFirstChild("HumanoidRootPart")
                    
                    if hum and hrp and hum.Health > 0 then
                        local targets = getTargets(targetMob)
                        if #targets == 0 and qData and qData.AltTarget then
                            targets = getTargets(qData.AltTarget)
                        end
                        
                        local target = nil
                        local bestDist = math.huge
                        for _, t in ipairs(targets) do
                            local d = (t.Root.Position - hrp.Position).Magnitude
                            if d < bestDist then
                                bestDist = d
                                target = t
                            end
                        end
                        
                        if target and target.Humanoid.Health > 0 then
                            local style = getEquippedStyle()
                            while State.Running and State.AutoQuestTasks and target.Humanoid and target.Humanoid.Health > 0 do
                                local currentQ = getActiveQuest()
                                if currentQ and currentQ.Complete then break end
                                
                                local tPos = target.Root.Position
                                hrp.CFrame = CFrame.lookAt(tPos + Vector3.new(0, State.MobHeightOffset, 0), tPos)
                                hrp.AssemblyLinearVelocity = Vector3.zero
                                
                                if RemoteEvent then
                                    RemoteEvent:FireServer("Combat_Service", style, combo, false, 0, false, nil)
                                end
                                if mouse1click then mouse1click() end
                                combo = combo % 5 + 1
                                task.wait(State.MobAttackDelay)
                            end
                        else
                            local spawnPos = RegionSpawns[targetMob]
                            if spawnPos then
                                safeTeleport(spawnPos)
                                task.wait(1.2)
                            else
                                task.wait(0.5)
                            end
                        end
                    end
                end
            else
                task.wait(1.0)
            end
        else
            task.wait(0.3)
        end
    end
end)

task.spawn(function()
    local combo = 1
    while State.Running do
        if State.FastAttack and not State.AutoFarmMobs and not State.AutoFarmBoss and not State.AutoQuestTasks then
            local style = getEquippedStyle()
            if RemoteEvent then
                RemoteEvent:FireServer("Combat_Service", style, combo, false, 0, false, nil)
            end
            if mouse1click then mouse1click() end
            combo = combo % 5 + 1
            task.wait(0.1)
        else
            task.wait(0.2)
        end
    end
end)

task.spawn(function()
    local playerValues = ReplicatedStorage:FindFirstChild("Player_Service")
        and ReplicatedStorage.Player_Service:FindFirstChild("Values")
        and ReplicatedStorage.Player_Service.Values:FindFirstChild(lp.Name)
        
    while State.Running do
        if State.AutoCompleteTraining then
            if not playerValues or not playerValues.Parent then
                playerValues = ReplicatedStorage:FindFirstChild("Player_Service")
                    and ReplicatedStorage.Player_Service:FindFirstChild("Values")
                    and ReplicatedStorage.Player_Service.Values:FindFirstChild(lp.Name)
            end
            
            local tr = playerValues and playerValues:FindFirstChild("Training")
            if tr then
                task.wait(0.35)
                if RemoteEvent then
                    RemoteEvent:FireServer("training_signaler", "Stop", true)
                end
                local timeout = 0
                while State.Running and State.AutoCompleteTraining and playerValues and playerValues:FindFirstChild("Training") and timeout < 20 do
                    task.wait(0.1)
                    timeout = timeout + 1
                end
                task.wait(0.5)
            else
                local char = lp.Character
                local hrp = char and char:FindFirstChild("HumanoidRootPart")
                local targetPos = WorkoutTeleports and SelectedWorkout and WorkoutTeleports[SelectedWorkout]
                
                if hrp and targetPos and (targetPos - hrp.Position).Magnitude > 20 then
                    safeTeleport(targetPos)
                    task.wait(0.8)
                end
                
                if setrobloxinput then pcall(function() setrobloxinput(true) end) end
                if keypress then
                    pcall(function()
                        keypress(116)
                        keypress(84)
                    end)
                end
                
                local holdTimer = 0
                while State.Running and State.AutoCompleteTraining and holdTimer < 14 do
                    if playerValues and playerValues:FindFirstChild("Training") then
                        break
                    end
                    task.wait(0.1)
                    holdTimer = holdTimer + 1
                end
                
                if keyrelease then
                    pcall(function()
                        keyrelease(116)
                        keyrelease(84)
                    end)
                end
                task.wait(0.3)
            end
        else
            task.wait(0.4)
        end
    end
end)

task.spawn(function()
    local skillKeys = {
        Enum.KeyCode.Z.Value,
        Enum.KeyCode.X.Value,
        Enum.KeyCode.C.Value,
        Enum.KeyCode.V.Value,
        Enum.KeyCode.B.Value
    }
    while State.Running do
        if State.AutoSpamSkills then
            for _, k in ipairs(skillKeys) do
                if not State.Running or not State.AutoSpamSkills then break end
                if keypress and keyrelease then
                    keypress(k)
                    task.wait(0.05)
                    keyrelease(k)
                end
                task.wait(0.2)
            end
            task.wait(State.SkillSpamInterval)
        else
            task.wait(0.5)
        end
    end
end)

task.spawn(function()
    while State.Running do
        local char = lp.Character
        if char then
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum and State.WalkSpeed > 16 and hum.WalkSpeed ~= State.WalkSpeed then
                hum.WalkSpeed = State.WalkSpeed
            end
            if State.NoClip then
                for _, p in ipairs(char:GetChildren()) do
                    if p:IsA("BasePart") and p.Name ~= "HumanoidRootPart" then
                        p.CanCollide = false
                    end
                end
            end
        end
        task.wait(0.2)
    end
end)

UI:Notify({
    Title = "Slayers 2 Hub",
    Content = "Loaded successfully!",
    Type = "success",
    Duration = 3
})
